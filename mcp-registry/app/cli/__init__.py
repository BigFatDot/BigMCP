"""
CLI commands for BigMCP administration.

Available commands:
    python -m app.cli.rotate_keys status     # Check encryption status
    python -m app.cli.rotate_keys migrate    # Migrate credentials to latest key
    python -m app.cli.rotate_keys generate   # Generate a new encryption key
"""
